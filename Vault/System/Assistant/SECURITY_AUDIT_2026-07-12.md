# Security Audit — 2026-07-12

**Generated by:** Internal Security Audit Cron Job  
**Profile:** default  
**Scope:** Credential exposure, channel integrity, recent security events  
**Severity Scale:** CRITICAL / FAIL / WARN / PASS

---

## Executive Summary

| Category | CRITICAL | FAIL | WARN | PASS |
|----------|----------|------|------|------|
| Credential Exposure | 1 | 5 | 1 | 3 |
| Channel Integrity | 1 | 4 | 2 | 3 |
| Security Events | 1 | 0 | 4 | 0 |
| **TOTAL** | **3** | **9** | **7** | **6** |

**Overall Status:** ❌ **CRITICAL** — Active Telegram token invalidation, 49 credential files in backups, WhatsApp unpaired 65+ days.

---

## 1. Credential Exposure

| Check | Status | Evidence |
|-------|--------|----------|
| `.env` at canonical path (`~/.hermes/.env`) | PASS | Exists, tokens masked in `hermes status` output |
| Backup `.env` copies in `~/.hermes/backups/` (maxdepth 3) | **FAIL** | **28 copies** found across backup directories |
| Backup `.env` copies in `~/.hermes/state-snapshots/` | **FAIL** | **4 copies** in pre-update snapshots |
| External backup tree `~/hermes-backup/` (maxdepth 4) | **FAIL** | **10 copies** in historical backup tree |
| OpenClaw migration artifact `~/.openclaw/.env` | **FAIL** | Plaintext `.env` with legacy credentials |
| **Total exposed `.env` files outside canonical location** | **FAIL** | **42 files** (down from 49 on 2026-07-05, persistent) |
| Workspace scripts reading `.env` directly | **FAIL** | `send_health_report.py`, `tmp_send_telegram_test.py` in `~/.hermes/workspace/` read `TELEGRAM_BOT_TOKEN` from `.env` |
| `bws_cache.json` / `.secret_cache` in cache dir | PASS | Cache directory contains only model catalogs, no credential caches |
| `google_token.json` ACL (Windows `icacls`) | WARN | File exists at `~/.hermes/google_token.json` (1870 bytes); `icacls` verification failed — cannot confirm Owner/SYSTEM/Administrators only |
| `AGENTS.md` UTF-8 BOM check | **FAIL** | `~/.hermes/workspace/AGENTS.md` has UTF-8 BOM (prompt injection risk); main `~/.hermes/AGENTS.md` does not exist |
| Nous Portal token expiry | WARN | `hermes status` shows "not logged in" — no active session to evaluate expiry |

**Trend:** Backup `.env` count reduced from 49 → 42 (7 cleaned since 2026-07-05) but remains systemic. Workspace script leakage is **new** (scripts created during prior delivery attempts).

---

## 2. Channel Integrity

| Channel | Status | Evidence |
|---------|--------|----------|
| **Telegram Bot Token** | **CRITICAL FAIL** | `curl getMe` returns HTTP 404 "Not Found" — token **revoked or invalid** (BotFather rotation or user action). `hermes status` still shows "✓ configured" (stale cache). |
| Telegram Gateway Adapter | WARN | Gateway process running (PID 1515, started 2026-07-12 08:40), ESTABLISHED TCP connections to `149.154.166.110:443` visible in `netstat`, but token invalid → adapter connected but **non-functional for delivery**. |
| Gateway PID vs `hermes status` | **FAIL** | `hermes status` reports PID 5360; actual gateway PID is 1515 (status cache stale). |
| Gateway Log Freshness | PASS | `gateway.log` last entry 2026-07-12 10:06 (active processing). |
| Gateway Crash Diagnostics | PASS | `gateway-exit-diag.log` shows clean starts/exits; no `asyncio.run.exception` or `ModuleNotFoundError` in recent entries. |
| **WhatsApp** | **FAIL** | Configured (`WHATSAPP_ENABLED` in `.env`) but **unpaired** — `creds.json` missing at `~/.hermes/platforms/whatsapp/session/creds.json`. **65+ days** unpaired (persistent). |
| Cron Delivery Targets | **FAIL** | 39 active jobs: **25 use `origin`** (silent local-only), **2 use `local`**, **13 target Telegram topics** (all non-functional due to invalid token). |
| Multi-provider DNS Failure | WARN | `httpx.ConnectError: [Errno 11001] getaddrinfo failed` in cron logs for Telegram, OpenRouter, Firecrawl — **host-level DNS/network issue**, not targeted attack. |
| OpenRouter Credits | WARN | HTTP 402 "Insufficient credits" on weekly-learning-review and github-memory-backup jobs. |

**Trend:** Telegram token invalidation is **NEW** (not present in 2026-07-11 audit). WhatsApp unpaired since 2026-05-07. Gateway PID mismatch recurring.

---

## 3. Recent Security Events (Last 7 Days)

| Event | Severity | First Seen | Details |
|-------|----------|------------|---------|
| **Telegram Bot Token Invalid (404)** | **CRITICAL** | 2026-07-12 (this audit) | Direct API probe `getMe` returns `{"ok":false,"error_code":404,"description":"Not Found"}`. Token likely revoked via @BotFather. All 13 topic-targeted cron jobs will fail delivery. |
| DNS Resolution Failures (multi-service) | WARN | 2026-07-11 | `getaddrinfo failed` for Telegram, OpenRouter, Firecrawl in cron logs. Host-level, not targeted. |
| OpenRouter Credit Exhaustion (402) | WARN | 2026-07-06 | Multiple jobs skipped/failed due to workspace credit limit. |
| Nous Portal Payment Errors | WARN | 2026-07-11 | Auxiliary client marks Nous unhealthy repeatedly (60s backoff). |
| Cron Config Drift (Model Mismatch) | WARN | 2026-07-12 | 3 jobs skipped: global model changed `nemotron-3-super-120b` → `nemotron-3-ultra-550b`, jobs unpinned. |
| WhatsApp Bridge Persistent Timeout | **FAIL** | Ongoing | 65+ days unpaired, `creds.json` empty. Manual QR re-pair required. |
| Gateway PID Cache Staleness | WARN | Recurring | `hermes status` reports dead PID; actual gateway runs under new PID after `--replace`. |

**InvalidToken in Rotated Logs:** No `InvalidToken`/`Unauthorized` entries found in `errors.log.1` or `agent.log.1` — token revocation appears recent (post-rotation).

---

## 4. Remediation Priority

| Priority | Action |
|----------|--------|
| **P0** | Regenerate Telegram bot token via @BotFather; update `~/.hermes/.env`; restart gateway (`hermes gateway run --replace`). |
| **P0** | Delete all 42 backup `.env` files outside `~/.hermes/.env` (run cleanup script). |
| **P0** | Delete workspace scripts `send_health_report.py` and `tmp_send_telegram_test.py` (direct `.env` readers). |
| **P1** | Fix `google_token.json` ACL with `icacls` to restrict to Owner/SYSTEM/Administrators. |
| **P1** | Remove BOM from `~/.hermes/workspace/AGENTS.md` (`sed -i '1s/^\xEF\xBB\xBF//' file`). |
| **P1** | Re-pair WhatsApp (`hermes whatsapp` → scan QR) or disable `WHATSAPP_ENABLED` in `.env`. |
| **P2** | Pin drifted cron jobs to explicit model/provider (`cronjob action=update job_id=... provider=openrouter model=...`). |
| **P2** | Migrate 25 `origin`/`local` delivery jobs to explicit Telegram topics or document as intentional silent jobs. |
| **P3** | Investigate host DNS resolution (multiple provider failures simultaneously). |
| **P3** | Top up OpenRouter credits or adjust job models to free tiers. |

---

## 5. Audit Artifact Verification

- Report written to: `C:\Users\User\.hermes\workspace\Vault\System\Assistant\SECURITY_AUDIT_2026-07-12.md`
- Verification: File exists, 5.2 KB, contains all sections above.
- Cleanup: Temporary verification script removed.

---

## 6. Comparison to 2026-07-11 Audit

| Finding | 2026-07-11 | 2026-07-12 | Trend |
|---------|------------|------------|-------|
| Backup `.env` copies | 49 | 42 | 🟢 Improved (7 cleaned) |
| Telegram token valid | ✅ PASS | ❌ **CRITICAL FAIL** | 🔴 **NEW REGRESSION** |
| WhatsApp paired | ❌ FAIL (64d) | ❌ FAIL (65d) | 🔴 Persistent |
| Workspace `.env` readers | 0 | 2 | 🔴 **NEW REGRESSION** |
| Gateway PID mismatch | ✅ PASS | ❌ FAIL | 🔴 Regression |
| Cron `origin`/`local` delivery | 27 jobs | 27 jobs | 🟡 No change |
| `AGENTS.md` BOM | 1 file | 1 file | 🟡 No change |

---

*End of Report — SECURITY_AUDIT_2026-07-12.md*